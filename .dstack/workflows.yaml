workflows:
  - name: train
    provider: python==3.8
    requirements: requirements.txt
    script: app.py
    environment:
      PYTHONPATH: src
    artifacts:
      - folder
      - cache
    resources:
      gpu: ${{ tgpu }}
